# OS files
.DS_Store

# Editor
.vscode

# Logs
log.log

# Spell check
cspell.json

# Claude Code settings
.claude/settings.local.json

# Git worktrees
.worktrees/
.claude/worktrees

# Plan docs (ephemeral working documents, not committed)
/docs/plans/

# Generated boundary tiles for maps (build artifact from services/boundaries/)
/priv/static/data/boundaries.pmtiles

# === Elixir/Phoenix ===

# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
gallformers-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# Ignore digested/fingerprinted assets (generated by mix phx.digest)
# These have content hashes in filenames and .gz compressed versions
/priv/static/**/*-????????????????????????????????.*
/priv/static/**/*.gz

# Elixir langage server
.expert/

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# Database files
*.db
*.db-*
# Ignore all SQLite databases in priv/ (dev, test, migration artifacts, etc.)
/priv/*.sqlite
*.sqlite-shm
*.sqlite-wal

# Cached WCVP data (large, downloadable from Kew)
/priv/repo/data/wcvp/

# WCVP reconciliation reports (generated, date-stamped)
/priv/repo/data/reconciliation/

# Built WCVP SQLite database (generated by mix gallformers.wcvp.build_db)
/priv/data/

# Dialyzer PLT files (large, environment-specific)
/priv/plts/

# Request logs (development only, production uses /data/logs)
/priv/logs/

# API compiled binaries
api/bin/

# Environment secrets
.env
.env.local
.env*.local
.env.pg-load

# E2E test artifacts (Wallaby)
/test/screenshots/*.png
!/test/screenshots/.gitkeep

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl
