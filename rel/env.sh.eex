#!/bin/sh

# Fly.io internal DNS resolves to IPv6 addresses (fdaa::).
# The BEAM must use IPv6 for Erlang distribution to reach
# Fly Postgres via .flycast hostnames.
export ERL_AFLAGS="-proto_dist inet6_tcp"
